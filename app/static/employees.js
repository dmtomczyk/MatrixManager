const employeeOrganizationSelect = document.querySelector('#employee-organization');
const employeeManagerSelect = document.querySelector('#employee-manager');
const employeeTypeSelect = document.querySelector('#employee-type');
const employeeManagerHelp = document.querySelector('#employee-manager-help');
const employeeTable = document.querySelector('#employee-table');
const employeeOrgFilter = document.querySelector('#employee-org-filter');
const employeeForm = document.querySelector('#employee-form');
const toast = document.querySelector('#toast');

let organizations = [];
let employees = [];

const escapeHtml = (value = '') => String(value).replace(/[&<>"']/g, (char) => ({ '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;', "'": '&#39;' }[char] || char));
const apiFetch = async (url, options = {}) => { const response = await fetch(url, { headers: { 'Content-Type': 'application/json' }, ...options }); if (!response.ok) { const err = await response.json().catch(() => ({})); throw new Error(err.detail || 'Request failed'); } if (response.status === 204) return null; return response.json(); };
const showToast = (message) => { toast.textContent = message; toast.classList.add('show'); setTimeout(() => toast.classList.remove('show'), 2200); };
const getLeaderEmployees = (currentEmployeeId = null) => employees.filter((emp) => emp.employee_type === 'L' && emp.id !== currentEmployeeId).sort((a,b)=>a.name.localeCompare(b.name));
const getCurrentEmployeeTypeValue = () => employeeTypeSelect?.value || 'IC';
const syncManagerFieldState = () => { const isIc = getCurrentEmployeeTypeValue() === 'IC'; employeeManagerSelect.required = isIc; if (employeeManagerHelp) employeeManagerHelp.textContent = isIc ? 'Required for ICs.' : 'Optional for leaders.'; };
const renderEmployees = () => { const selectedOrg = employeeOrgFilter?.value || ''; const rows = employees.filter((emp) => !selectedOrg || String(emp.organization_id) === selectedOrg).map((emp) => `<tr><td>${escapeHtml(emp.name)}</td><td>${escapeHtml(emp.role || '')}</td><td>${escapeHtml(emp.employee_type || 'IC')}</td><td>${escapeHtml(emp.organization_name || '')}</td><td>${escapeHtml(emp.manager_name || '—')}</td><td>${escapeHtml(emp.location || '')}</td><td>${emp.capacity?.toFixed(1) || '1.0'}</td><td class="actions"><button type="button" data-action="edit-employee" data-id="${emp.id}">Edit</button><button type="button" class="secondary" data-action="delete-employee" data-id="${emp.id}">Delete</button></td></tr>`).join(''); employeeTable.innerHTML = rows || '<tr><td colspan="8">No employees match this filter.</td></tr>'; };
const updateOrganizationSelect = () => { const options = organizations.map((org) => `<option value="${org.id}">${escapeHtml(org.name)}</option>`).join(''); employeeOrganizationSelect.innerHTML = '<option value="">Select organization</option>' + options; const filterOptions = ['<option value="">All organizations</option>'].concat(organizations.map((org) => `<option value="${org.id}">${escapeHtml(org.name)}</option>`)).join(''); employeeOrgFilter.innerHTML = filterOptions; };
const updateManagerSelect = (selectedId = '', currentEmployeeId = null) => { const leaders = getLeaderEmployees(currentEmployeeId); employeeManagerSelect.innerHTML = ['<option value="">No manager</option>'].concat(leaders.map((emp) => `<option value="${emp.id}">${escapeHtml(emp.name + (emp.organization_name ? ` · ${emp.organization_name}` : ''))}</option>`)).join(''); employeeManagerSelect.value = selectedId && leaders.some((emp) => String(emp.id) === String(selectedId)) ? String(selectedId) : ''; syncManagerFieldState(); };
const resetForm = () => { employeeForm.reset(); employeeForm.querySelector('input[name="entity_id"]').value = ''; employeeForm.querySelector('button[type="submit"]').textContent = 'Save Employee'; employeeTypeSelect.value = 'IC'; syncManagerFieldState(); updateManagerSelect(); };
const loadOrganizations = async () => { organizations = await apiFetch('/organizations'); updateOrganizationSelect(); };
const loadEmployees = async () => { employees = await apiFetch('/employees'); renderEmployees(); updateManagerSelect(employeeManagerSelect?.value || '', Number(employeeForm?.querySelector('input[name="entity_id"]')?.value) || null); };
const populateEmployeeForm = (id) => { const employee = employees.find((e) => e.id === Number(id)); if (!employee) return; employeeForm.name.value = employee.name; employeeForm.role.value = employee.role || ''; employeeTypeSelect.value = employee.employee_type || 'IC'; employeeOrganizationSelect.value = employee.organization_id || ''; employeeForm.location.value = employee.location || ''; employeeForm.capacity.value = employee.capacity || 1; employeeForm.querySelector('input[name="entity_id"]').value = employee.id; employeeForm.querySelector('button[type="submit"]').textContent = 'Update Employee'; updateManagerSelect(employee.manager_id || '', employee.id); syncManagerFieldState(); };
employeeForm.addEventListener('submit', async (event) => { event.preventDefault(); const formData = new FormData(employeeForm); const organizationId = Number(formData.get('organization_id')); if (!organizationId) return alert('Select an organization for this employee.'); const payload = { name: formData.get('name').trim(), role: formData.get('role').trim() || null, employee_type: getCurrentEmployeeTypeValue(), location: formData.get('location').trim() || null, capacity: Number(formData.get('capacity')) || 1, organization_id: organizationId, manager_id: employeeManagerSelect.value ? Number(employeeManagerSelect.value) : null }; const id = formData.get('entity_id'); try { if (id) { await apiFetch(`/employees/${id}`, { method: 'PUT', body: JSON.stringify(payload) }); showToast('Employee updated'); } else { await apiFetch('/employees', { method: 'POST', body: JSON.stringify(payload) }); showToast('Employee added'); } resetForm(); await loadEmployees(); } catch (err) { alert(err.message); } });
employeeTable.addEventListener('click', async (event) => { const btn = event.target.closest('button[data-action]'); if (!btn) return; const { action, id } = btn.dataset; if (action === 'edit-employee') populateEmployeeForm(id); if (action === 'delete-employee') { if (!confirm('Delete this employee and related assignments? Direct reports will become unassigned.')) return; await apiFetch(`/employees/${id}`, { method: 'DELETE' }); showToast('Employee deleted'); await loadEmployees(); } });
employeeTypeSelect.addEventListener('change', syncManagerFieldState);
employeeOrgFilter.addEventListener('change', renderEmployees);
(async function init() { await loadOrganizations(); await loadEmployees(); syncManagerFieldState(); })();
